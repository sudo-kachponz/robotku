# @teachablemachine/image@0.8.5 declares a stale peer (@tensorflow/tfjs@1.3.1) but
# runs fine against tfjs 4.x (what the whole app uses). legacy-peer-deps lets
# `npm ci` in CI resolve without a spurious ERESOLVE. Revisit if TM ships a fixed peer.
legacy-peer-deps=true
